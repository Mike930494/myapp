<%= form_for(@h_igh_score) do |f| %>
  <% if @h_igh_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@h_igh_score.errors.count, "error") %> prohibited this h_igh_score from being saved:</h2>

      <ul>
      <% @h_igh_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %><br>
    <%= f.text_field :game %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
